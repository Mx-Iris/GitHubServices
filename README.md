# GitHubServices

GitHubServices is a multi-platform Swift package that provides GitHub REST and GraphQL models, networking clients, and higher-level repository services.

The `GitHubModels` product exposes `Codable`, compiler-checked `Sendable` model types that can safely cross Swift concurrency isolation boundaries.
